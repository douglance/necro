# Append agent output to task files

After `necro watch --agent` finishes a headless run, append that agent stdout to the task markdown. Follow the file if the agent moved it. Keep showing the output on the watcher stdout. Verify with tests and update README.
