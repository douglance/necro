# Write headless-yolo-smoke.txt

Write headless-yolo-smoke.txt at the repository root with exactly this line:

yolo-watch spawned grok

Then run `necro start` and `necro done` for this task. Do not change any other files.
